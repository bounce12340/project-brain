# v7.1：AI 匯入拆分粒度修正（使用者回饋）

> 使用者實測回饋：單筆公告的內容被拆成好幾個項目。要求：一則公告匯成一筆，內容做摘要，公告內的多個項目用條列式呈現在重點欄。

## 1. 行為修正

- 「一筆 reg_entry」的定義寫死進 LLM prompt：**一則獨立公告**（有自己的發文日期／文號／公告標題）才算一筆。同一公告內的多個修正條文、品項、附表、子項目 → **一律併入該筆的 key_points 條列**，絕不拆成多筆。
- key_points 結構：第一行為 1～2 句摘要（不帶符號），之後每個項目一行、以「•」開頭。
- prompt 內附一組 few-shot 範例：一則含 3 個修正項目的公告 → 輸出恰好 1 筆、key_points 為摘要＋3 條列。

## 2. 模式開關（UI＋API）

- AI 匯入 drawer 加單選，**預設「單則公告」**：
  - 單則公告：prompt 強制輸出**恰好 1 筆**（整份輸入視為同一則公告，全部內容摘要＋條列）。後端若 LLM 仍回多筆 → 自動合併為 1 筆（取最早日期與第一個標題，key_points 串接）。
  - 多則彙整：沿用既有多筆拆解，但套用 §1 的公告邊界定義（不同日期或不同公告標題才拆）。
- `ai-extract` API 加 `mode: 'single' | 'multi'`（預設 single）；切塊合併邏輯在 single 模式下同樣強制歸一。

## 3. 測試與驗收（ACCEPTANCE-V7-1.md）

1. 調整／新增 vitest ≥4：single 模式多筆自動歸一、multi 模式邊界定義、key_points 摘要＋條列結構驗證、mode 預設值。
2. typecheck／test／build 全綠；deploy 成功。
3. 正式站 E2E（demo RA/PV 帳號，事後清理）：
   a. 用「一則含 3 個修正項目的自擬公告」以**單則模式**實測 → 恰好 1 筆、key_points 含摘要行＋3 個「•」項目。
   b. 用「兩則不同日期公告」以**多則模式**實測 → 2 筆。
   c. 迴歸：既有 627 筆不變、未登入 401。
4. /help 對應段落更新；結果如實記錄。

## 4. 紀律

同前：不碰 usr_admin 與 secrets、小步 commit、不問問題。
