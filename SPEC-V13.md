# v13：欄位說明提示（?）＋完整新手教程

> 使用者回饋：「里程碑、歷程事件、專案資訊」等標題語意相近，看不出各自目的與該填什麼；也需要一套讓第一次使用專案系統／法規系統的人能完整走一遍的教程。

## 1. HelpTip 元件（可重用的「?」提示）

- 新增 `src/components/HelpTip.tsx`：`<HelpTip topic="milestones" />`。
- 外觀：標題右側的小圓形「?」（14px，star-dim 色，hover 轉 psi），**不干擾版面**；雙主題一致。
- 互動：hover 或 focus 顯示氣泡；行動裝置以點擊切換；Esc 關閉；`aria-describedby` 綁定內容；鍵盤 Tab 可達（`role="button"`, `tabindex=0`）。
- 氣泡內容固定三段式（缺項可省略）：
  1. **是什麼**：一句話定義（≤30 字）
  2. **填什麼**：具體指引，含一個真實例子（≤40 字）
  3. **與 X 的差別**：僅在容易混淆時出現（≤40 字）
- 內容來源：`src/help-topics.ts` 匯出 `HELP_TOPICS: Record<TopicKey, {zh: HelpContent; en: HelpContent}>`；型別保證中英欄位齊備，另加 vitest 檢查兩語言 key 與段落完整性一致。

## 2. 必須配置 HelpTip 的位置（逐一實作，不得遺漏）

**專案總覽**：專案目標｜整體進度與進度模式（手動／自動）｜里程碑｜歷程事件｜專案資訊（起始日／目標日）｜成員｜可見性｜狀態｜季度目標（Objective）｜Key Results｜AI 風險預測

**任務頁**：階段｜任務卡｜起始日／到期日｜前置依賴｜四視圖切換（看板／清單／日曆／甘特各一句用途）｜未排程

**進度紀錄**：進度紀錄本身｜AI 快寫｜AI 連動建議

**專屬模組**：收案目標與逐日登錄｜BD 案件狀態／歷程／費用｜QA 證照效期｜CCR 變更管制

**其他**：檔案｜自動化規則｜待辦｜通知

**法規動態**：公告日期（對比施行日）｜產品線｜類別｜TFDA 草稿與待審核｜AI 匯入的單則／多則模式｜附件

**報表**：週報／月報產生範圍｜時間軸泳道

## 3. 概念對照表（本版最重要的內容產出）

於 `/help` 新增顯著章節「**這四個東西差在哪**」，以表格呈現，並在相關 HelpTip 的「差別」段落引用同一組定義（單一事實來源，寫在 `help-topics.ts`）：

| 概念 | 語意 | 有無日期 | 影響進度 | 典型例子（用 RA 註冊案） |
|---|---|---|---|---|
| **任務** | 要做的工作，可指派、可排程 | 起訖日 | ✅ 計入 | 「收到CTD文件進行審查」 |
| **里程碑** | 未來要達成的關鍵節點 | 到期日 | ✅ 計入 | 「2026-08-14 收到CTD文件」 |
| **歷程事件** | 已經發生的事實紀錄 | 事件日 | ❌ 不計入 | 「2025-12-09 CDE第一次諮詢」 |
| **進度紀錄** | 敘事日誌，說明這段期間發生什麼 | 撰寫時間 | ❌ 不計入（可觸發連動建議） | 「本週完成資料彙整，下週送件」 |
| **待辦** | 個人層級的私人事項 | 到期日 | ✅ 計入（若關聯專案） | 「提醒自己追 Hina 回信」 |

## 4. 完整教程（兩套互動導覽＋書面手冊）

### 4a. 互動導覽（擴充既有 OnboardingTour）

- 拆成兩套具名導覽：
  - **專案系統入門**（12–16 步）：導覽列 → 儀表板 KPI 與動態 → 專案清單與新增 → 專案總覽（目標／進度模式／里程碑／歷程事件） → 任務四視圖 → 任務抽屜（依賴與日期接龍） → 進度紀錄與 AI 連動 → 檔案 → 自動化 → 報表 → 時間軸 → 完成。
  - **法規系統入門**（8–10 步）：法規動態清單 → 公告日期與篩選（年／月／產品線） → 待審核草稿與批次操作 → AI 匯入（單則／多則、多檔） → 附件下載 → 刪除與駁回墓碑 → 完成。
- 首次登入時顯示選擇卡：「先看專案系統」「先看法規系統」「略過」（沿用 `onboarding_done` 旗標；使用者可從 /help 隨時重播任一套）。
- 每步文案 ≤40 字、指向真實 UI 元素（沿用既有 spotlight 機制與 `data-tour` 屬性；缺少屬性的目標元素要補上）。
- 導覽進行中若該頁面元素不存在（例如非 QA 組專案沒有 QA 籤），該步自動跳過不中斷。

### 4b. 書面手冊（/help 重構）

- `/help` 改為三段結構：
  1. **快速上手**：三個情境式教學，每個 5–8 步、含具體輸入例子——
     - 情境 A：建立一個查驗登記專案並排出時程（用階段模板 → 建任務 → 設依賴讓日期接龍 → 設里程碑）
     - 情境 B：每週回報進度（寫進度 → AI 快寫 → 連動建議勾選 → 看儀表板與週報）
     - 情境 C：處理一則 TFDA 公告（待審核 → 檢視重點 → 核准或編輯 → 用月份篩選回查）
  2. **概念對照表**（§3）
  3. **功能索引**：既有的逐功能說明（保留現有內容，補上新功能）
- 每段標題可深連結（`/help#quickstart-a` 等），HelpTip 氣泡底部提供「看完整教學 →」連結至對應段落。

## 5. i18n 與品質

- 所有新字串中英齊備，key-parity 測試須綠；HelpTip 內容另有專屬 parity 測試（每個 topic 的 zh/en 三段落齊備）。
- 新增測試：`help-topics.ts` 的 topic 清單與 §2 配置清單一致（避免有 tooltip 卻沒內容、或有內容卻沒放）——以常數陣列比對。
- 效能：HelpTip 內容為靜態物件，不得因此新增網路請求；bundle 增量記錄。

## 6. 驗收（ACCEPTANCE-V13.md；demo 資料，不得動使用者真實專案）

1. typecheck／test／build 全綠（新增測試 ≥10）；deploy 成功（無 migration）。
2. built asset 驗證：HelpTip 元件存在、§2 所列位置的 topic key 全數出現、/help 三段結構與深連結錨點存在、兩套導覽的步驟資料存在。
3. 正式站 E2E（demo 帳號）：`/help` 200 且含概念對照表關鍵字（任務／里程碑／歷程事件／進度紀錄／待辦）；切換 EN 後對應英文字串存在。
4. 迴歸：401、published 631、TFDA 草稿與 tombstone 不變、真實專案 fingerprints 不變、title。
5. 視覺／互動（hover 氣泡、鍵盤操作、行動裝置點擊、雙主題）由執行者以瀏覽器確認並記錄；無瀏覽器則如實標示待派工者驗證。
6. README 補一句；DECISIONS 增量；結果如實寫入 ACCEPTANCE-V13.md。

## 7. 紀律

同 SPEC-V4 §6。commit：help-topics 內容→HelpTip 元件→逐頁配置→/help 重構→雙導覽→驗收。**內容品質是本版重點**：文案要具體（帶真實例子）、不得空泛（禁止「用於管理里程碑」這類同義反覆）。
