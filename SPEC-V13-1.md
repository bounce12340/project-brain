# v13.1：甘特橫條加粗＋依階段著色

> 使用者需求：甘特圖要能一眼看出「這段期間在跑哪一關」——任務橫條加粗，並依所屬階段以不同顏色區分，附圖例。

## 1. 依階段著色

- 階段（stages）本來就有 `color` 欄位（看板圓點在用）；甘特任務橫條改用**該任務所屬階段的顏色**填充，取代目前的單一 psi 藍。
- 色彩處理：以階段色為主色，`opacity` 0.85；**已完成任務**降至 0.45 並保留既有勾記，維持可辨識。
- 對比保護：文字標籤仍在橫條左側的標籤欄（不疊在色條上），因此階段自訂色不會造成可讀性問題；若橫條上仍有任何文字，須改為以亮度判斷自動選黑／白字。
- 里程碑（實心金菱形）與歷程事件（空心淡金菱形）維持 v12.4 樣式**不受階段色影響**——它們不屬於任何階段。
- 亮色主題：階段色若過淺，加 1px 同色深階描邊確保在象牙底上可見。

## 2. 圖例

- 甘特圖上方新增水平圖例列：每個有任務的階段一個色塊＋階段名稱（依階段 position 排序），另含「里程碑」「歷程事件」兩個符號說明。
- 圖例與甘特同步捲動時保持可見（隨圖表容器，不吸附）。
- 階段數多時圖例自動換行。

## 3. 加粗

- 任務橫條高度 14px → **18px**；列高對應調整避免擁擠（rowHeight 46 → 52）。
- 里程碑／歷程事件菱形等比放大以維持視覺平衡。
- 全域 `/timeline` 的專案展開任務條沿用同樣的加粗與階段色規則。

## 4. 測試（vitest ≥4）

階段色對應（任務 → 階段 → 色）、無階段或階段無色時的 fallback（用預設 psi）、完成任務透明度、圖例資料只含實際有任務的階段且依 position 排序。

## 5. 驗收（ACCEPTANCE-V13-1.md）

1. typecheck／test／build 全綠；deploy 成功（無 migration）。
2. built asset 驗證：橫條高度 18、rowHeight 52、階段色套用邏輯與圖例渲染存在。
3. 視覺確認（暗色／亮色／窄螢幕）：多階段 demo 專案的甘特可清楚區分不同階段任務；由執行者以瀏覽器確認並記錄，無瀏覽器則如實標示。
4. 迴歸：401、published 631、TFDA 草稿與 tombstone 不變、真實專案 fingerprints 不變、title。
5. 結果如實寫入 ACCEPTANCE-V13-1.md；DECISIONS 增量。

## 6. 紀律

同 SPEC-V4 §6。單一小步 commit；不碰 usr_admin、secrets 與真實資料。**已發佈的 migration 不得就地修改**（本版無 migration）。
