# v9.1：登入頁水晶重塑（使用者截圖回饋）

> 使用者回饋：現在的水晶（兩個旋轉方形疊出的寬菱形）左右太寬、且絕對定位壓到「艾爾水晶-專案進度」標題。要求：**上下較長、左右較窄的水晶體，有明顯切割面，不得遮擋任何文字**。

## 1. 新水晶設計（改為 SVG，取代現有 CSS 方形疊加）

- 形狀：縱長水晶柱（Khaydarin 碎晶），viewBox `0 0 100 180`，外輪廓六邊碎片形，參考頂點：`(50,4) (78,42) (70,130) (50,176) (30,130) (22,42)`（可微調更修長，寬高比不得超過 0.6）。
- **切割面**：以中央縱稜線 `(50,4)–(50,176)` 與兩條橫向折線（約 y=42 與 y=130）把內部分成 6 個 facet polygon，各自填不同深淺的 psi 藍（例：左上 `#1A6FA8/0.55`、右上 `#35C8FF/0.85`、左中 `#12507E/0.6`、右中 `#2AA6DB/0.75`、左下 `#0E3A5C/0.6`、右下 `#1E86C2/0.8`），面與面交界自然形成稜線；右上 facet 加一小塊白色高光三角（opacity 0.5）做玻璃感。
- 外框：`#C8A24A` 金色 stroke 1.5px；水晶後方一層柔和 psi 徑向光暈。
- 動畫：整體 4s 呼吸（光暈 opacity 脈動）；`prefers-reduced-motion: reduce` 時靜止。兩主題皆需成立（亮色下光暈調淡）。

## 2. 版面修正（關鍵）

- 水晶改為**正常文流元素**（非 absolute），置於「UIC INTERNAL」眉標之上，高度約 72–88px，與下方文字保持 ≥16px 間距；任何視窗寬度（375px 起）都不得與標題、副標、表單重疊。
- 登入頁與註冊頁共用的 auth 版面一併套用。

## 3. 驗收（ACCEPTANCE-V9-1.md）

1. typecheck／test／build 全綠；deploy 成功。
2. 靜態驗證：built asset 含新 SVG（polygon 數 ≥6＋金框）；舊的方形疊加樣式已移除。
3. 迴歸：GET / 200、title 不變、未登入 401。
4. 佈局斷言：以 DOM 結構確認水晶為文流元素且位於標題之前（無 absolute 疊壓）；寫入驗收檔。
5. 視覺由派工者以瀏覽器確認（雙主題），本檔記錄可自驗部分。

## 4. 紀律

同 SPEC-V4 §6。小步 commit；不碰 usr_admin 與 secrets。
