# v13.2：里程碑／歷程事件可跨期間（畫橫條）＋甘特字級與完成狀態視覺

> 使用者實測回饋：(1) 甘特時間刻度字太小；(2)(4) 里程碑與歷程事件只畫出菱形結點、沒有橫條——因為兩者目前各只有一個日期（到期日／事件日），單點無法表達長度，但法規業許多節點本質是一段期間（例：CDE 收案 2026-02-06 → 審查回覆 2026-03-23）；(3) 完成與未完成僅以透明度區分，辨識度不足。

## 1. 里程碑／歷程事件支援期間（migration 0013，**新檔，不得就地改既有 migration**）

- `milestones` 加欄：`end_date` TEXT NULL（語意：期間結束日；既有資料一律 NULL）。
- 既有 `due_date` 語意不變：里程碑＝目標／到期日、歷程事件＝事件（起始）日。加了 `end_date` 就代表「這是一段期間」。
- 後端驗證：`end_date` 若有值必須 ≥ `due_date`，否則 422；清空 `end_date` 允許（回到單點）。
- API 回傳含 `end_date`；批次匯入（IMPORT.md）與 progress-links 的 `milestones[]`／`events[]` 一併支援選填 `end_date`，AI 從文字抽到明確期間（例：「為期一個月」「2/6 至 3/23」）時可建議填入，仍受既有「預設不勾、人工套用」原則規範。

## 2. 甘特呈現

- **有 `end_date`** → 畫橫條（`due_date` → `end_date`），高度同任務條（18px）：
  - 里程碑：金色（`gold`）填充、深金描邊。
  - 歷程事件：淡金（`gold-dim`）填充 0.5 透明、虛線描邊（維持「已發生」的次要感）。
  - 條的**起點與終點各畫一個小菱形端點**，維持與單點樣式的家族感。
- **無 `end_date`** → 維持現行菱形點（不改）。
- 圖例補上「里程碑期間」「歷程期間」兩個樣式說明。
- 日曆：有期間者在起訖之間每日顯示標記（沿用既有標記樣式，不新增顏色系）。
- 專案總覽的里程碑／歷程事件清單顯示日期範圍（`2026-02-06 ~ 2026-03-23`），單點則僅顯示單一日期。表單加「結束日（選填）」欄位，附 HelpTip：「填了就會在甘特畫成橫條，不填則是單點節點」。

## 3. 甘特字級與可讀性

- 頂部時間刻度（月／週標記）字級 11px → **14px、font-weight 600**；月份標記再加深顏色（gold-bright／亮色主題用 gold-dim）以區隔週刻度。
- 左側任務／里程碑標籤字級提升至 **14px**、font-weight 500；標籤欄寬度由 190 → 210 以容納。
- today 線標籤（若有）同步放大。
- 全域 `/timeline` 沿用相同字級規則。

## 4. 完成與未完成的視覺區分（取代僅靠透明度）

- **未完成任務**：階段色實心（現行）。
- **完成任務**：階段色降至 30% 透明 ＋ **疊加 45° 斜線紋理**（SVG `<pattern>`，同色深階）＋ 條左端保留勾記 ✓。斜線紋理讓完成狀態在黑白列印與色盲情境下也能辨識。
- **逾期未完成**（due_date < 今天且未完成）：條的右端加 `danger` 色 3px 直立標記（不整條變紅，避免與階段色打架）。
- 圖例補「完成（斜紋）」「逾期（紅端標）」說明。
- 里程碑／歷程事件的完成語意不同（歷程事件恆為已發生），不套用斜紋；里程碑逾期未完成同樣加紅端標。

## 5. 測試（vitest ≥6）

`end_date` 驗證（≥ due_date、可清空）、有無 end_date 的渲染分支資料、逾期判定（台北日界）、完成任務紋理旗標、圖例項目依實際內容產生、AI 建議帶 end_date 的清洗。

## 6. 驗收（ACCEPTANCE-V13-2.md；demo 資料，不得動使用者真實專案）

1. typecheck／test／build 全綠；migration 0013 remote 套用（**確認為新檔且 `sqlite_master` 實際有 end_date 欄**，不可只信 vitest）；deploy 成功。
2. E2E（新建 demo 專案，事後刪除）：建里程碑（due 2026-09-01、end 2026-09-30）→ API 回傳 end_date → 甘特資料集判定為 bar；建歷程事件（2026-02-06 ~ 2026-03-23）→ 同樣為 bar；建單點里程碑 → 維持 diamond；`end_date` < `due_date` → 422。
3. built asset 驗證：時間刻度字級 14／weight 600、標籤 14、斜線 pattern 定義、逾期紅端標邏輯存在。
4. 視覺確認（暗色／亮色／窄螢幕）：由執行者以瀏覽器確認並記錄。
5. 迴歸：401、published 631、TFDA 草稿與 tombstone 不變、真實專案 fingerprints 不變、title。
6. README／help 更新（說明單點 vs 期間的填法）；DECISIONS 增量；結果如實寫入 ACCEPTANCE-V13-2.md。

## 7. 紀律

同 SPEC-V4 §6。commit：migration→後端與驗證→甘特渲染→字級與完成視覺→驗收。**已發佈的 migration 一律不得就地修改，必須新增編號**。
