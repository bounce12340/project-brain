# v12.5：任務抽屜依賴優先＋起日自動接續前置任務

> 使用者需求：點開任務時「前置依賴」應該先出現、先選；選完之後起始日自動接續前置任務的到期日，省去逐一挑日期，但仍可手動改。

## 1. 版面順序（任務抽屜）

現行欄位順序改為：**標題 → 描述 → 負責人 → 前置依賴 → 起始日／到期日 → 完成勾選 → 留言／附件／AI 摘要**。

- 「前置依賴」區塊上移到日期之前，並加一行說明：「選擇前置任務後，起始日會自動接續其到期日」。
- 其餘功能與權限不變。

## 2. 起日自動接續（核心）

- 觸發時機：**新增或變更前置依賴時**。
- 計算：取所有前置任務中**最晚的 `due_date`**（finish-to-start 慣例），建議起始日 ＝ 該日期 **＋1 天**（Asia/Taipei 日曆日）。
- 套用規則（防止覆蓋使用者心血）：
  - 起始日**為空** → 直接帶入建議值，欄位旁顯示提示「已依前置任務帶入，可修改」。
  - 起始日**已有值且是自動帶入的**（未被手動編輯過）→ 更新為新的建議值。
  - 起始日**已被使用者手動輸入／修改過** → **不覆蓋**，改在欄位下方顯示一行建議「建議起始日 YYYY-MM-DD（依前置任務）」＋「套用」小按鈕，由使用者決定。
  - 以元件內 `startDateTouched` 狀態追蹤是否手動編輯過（使用者手動改動即設為 true；抽屜重新開啟時重置）。
- 連動到期日：若任務**原本已同時有起訖日**（存在工期），起始日被自動移動時，到期日同步平移相同天數以保留工期；若原本沒有到期日則保持空白。
- 前置任務**沒有 `due_date`** → 無法計算，顯示提示「前置任務尚未設定到期日，無法自動接續」，不做任何自動填值。
- 移除全部前置依賴 → 不清空已填日期（避免誤刪資料），僅移除提示。
- 所有自動帶入僅發生在前端表單狀態；**必須按既有儲存流程才寫入資料庫**（不得自動送出）。

## 3. 依賴選單體驗

- 依賴下拉／多選清單顯示「任務標題（到期日）」，未設到期日者標示「未設定」，方便判斷能否接續。
- 既有循環依賴偵測與 422 行為不變。

## 4. i18n 與樣式

- 新增字串（提示、建議、套用按鈕）中英齊備，key-parity 測試須綠；雙主題樣式一致。

## 5. 測試（vitest ≥6）

日期計算純函式：多前置取最晚到期日、+1 天跨月與跨年、前置無到期日回 null、工期保留平移（起訖同時存在）、手動編輯後不覆蓋（touched 旗標邏輯）、移除依賴不清空日期。

## 6. 驗收（ACCEPTANCE-V12-5.md；demo 資料，不得動使用者真實專案）

1. typecheck／test／build 全綠；deploy 成功（無 migration）。
2. E2E（新建 demo 專案，事後刪除）：建 A（due 2026-09-10）與 B（無日期）→ B 設定依賴 A → 前端建議起始日 2026-09-11（以 built asset 邏輯或元件測試驗證）→ 儲存後 API 回傳 B.start_date=2026-09-11；再手動改成 2026-09-20 後變更依賴 → 不被覆蓋、顯示建議。
3. built asset 驗證：依賴區塊排序在日期欄位之前。
4. 迴歸：401、published 631、真實草稿與 tombstone 不變、真實專案 fingerprints 不變、title。
5. 結果如實寫入 ACCEPTANCE-V12-5.md；README／help 補一句；DECISIONS 增量。

## 7. 紀律

同 SPEC-V4 §6。小步 commit；不碰 usr_admin、secrets 與真實資料。**不得自動寫入資料庫**是本版最高原則。
